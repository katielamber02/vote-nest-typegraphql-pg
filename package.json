{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "name": "vote",
  "scripts": {
    "build:common": "npx lerna run build --scope='@vote/common'",
    "clean": "npx lerna clean --yes && rimraf node_modules"
  },
  "dependencies": {}
}
